# Closed shelf entries — archive

> Fictional stub. Exists so the anchor in `../ACTIVE.md` resolves.

Pointers only, no clocks. An entry arrives here once its lesson lives in a memory and the shelf
line has been compressed to one line. Nothing in this file generates work.
